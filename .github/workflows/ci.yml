name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

jobs:

  # ── Ready ────────────────────────────────────────────────────
  # Placeholder while the backend and frontend are being set up.
  # To replace this job with lint, test and docker-build once
  # backend/ and frontend/ exist in the repository.
  ready:
    name: CI ready
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: OK
        run: echo "CI pipeline is configured and ready."

  # ── Lint (uncomment only when backend/ is added) ──────────────────
  # lint:
  #   name: Lint
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 20
  #         cache: npm
  #         cache-dependency-path: backend/package-lock.json
  #     - run: npm ci
  #       working-directory: backend
  #     - run: npm run lint
  #       working-directory: backend

  # ── Test (uncomment only when backend/ and tests are added) ───────
  # test:
  #   name: Test
  #   runs-on: ubuntu-latest
  #   needs: lint
  #   services:
  #     postgres:
  #       image: postgres:16-alpine
  #       env:
  #         POSTGRES_USER: sf
  #         POSTGRES_PASSWORD: sf
  #         POSTGRES_DB: smartflow_test
  #       options: >-
  #         --health-cmd pg_isready
  #         --health-interval 5s
  #         --health-timeout 5s
  #         --health-retries 5
  #       ports:
  #         - 5432:5432
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 20
  #         cache: npm
  #         cache-dependency-path: backend/package-lock.json
  #     - run: npm ci
  #       working-directory: backend
  #     - run: npx prisma migrate deploy
  #       working-directory: backend
  #       env:
  #         DATABASE_URL: postgresql://sf:sf@localhost:5432/smartflow_test
  #     - run: npm test
  #       working-directory: backend
  #       env:
  #         DATABASE_URL: postgresql://sf:sf@localhost:5432/smartflow_test
  #         JWT_SECRET: ci_test_secret_not_for_production

  # ── Docker build (uncomment only when docker files are added) ──────
  # docker-build:
  #   name: Docker build
  #   runs-on: ubuntu-latest
  #   needs: test
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: docker/setup-buildx-action@v3
  #     - run: docker compose build
  #       env:
  #         POSTGRES_USER: sf
  #         POSTGRES_PASSWORD: sf
  #         POSTGRES_DB: smartflow_test
  #         JWT_SECRET: ci_test_secret_not_for_production